apiVersion: messaging.platform.ohanyere.io/v1alpha1
kind: SQSQueueClaim
metadata:
  name: claims-api-queue
  labels:
    app.kubernetes.io/name: claims-api
    finops.ohanyere.io/cost-center: payments
spec:
  environment: dev
  visibilityTimeoutSeconds: 30
  messageRetentionSeconds: 345600
  deadLetterQueue:
    enabled: true
    maxReceiveCount: 5
