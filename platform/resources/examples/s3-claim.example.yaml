apiVersion: storage.platform.ohanyere.io/v1alpha1
kind: S3BucketClaim
metadata:
  name: claims-api-bucket
  labels:
    app.kubernetes.io/name: claims-api
    finops.ohanyere.io/cost-center: payments
spec:
  environment: dev
  versioning: true
  encryption: platform-managed
  lifecycle:
    transitionToInfrequentAccessDays: 30
