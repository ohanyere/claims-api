apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: claims-api
  labels:
    app.kubernetes.io/name: claims-api
spec:
  hosts:
    - claims-api.dev.platform.ohanyere.internal
  gateways:
    - istio-system/platform-gateway
  http:
    - route:
        - destination:
            host: claims-api
            port:
              number: 80
          weight: 100
